<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Приглашение на свадьбу</title>
  <style>
    body {
      margin: 0;
      font-family: 'Arial', sans-serif;
      background: #f8f8f8;
      color: #333;
      text-align: center;
      padding: 2rem;
    }
    h1 {
      font-size: 2.5rem;
      color: #b44b4b;
    }
    .timer {
      font-size: 2rem;
      margin: 1rem 0;
    }
    .section {
      margin: 2rem 0;
    }
    iframe {
      border: none;
      width: 100%;
      max-width: 600px;
      height: 300px;
    }
    .rsvp {
      background: #b44b4b;
      color: white;
      padding: 1rem 2rem;
      border-radius: 10px;
      text-decoration: none;
      display: inline-block;
      margin-top: 1.5rem;
    }
  </style>
</head>
<body>
  <h1>Айгуль ❤️ Тимур</h1>
  <div class="section">
    <p>Мы будем счастливы видеть вас на нашей свадьбе!</p>
    <p><strong>25 августа 2025 года, в 17:00</strong></p>
  </div>

  <div class="section">
    <div class="timer" id="timer">Загрузка таймера...</div>
  </div>

  <div class="section">
    <h2>Место проведения:</h2>
    <p>Astana Grand Hall, г. Астана</p>
    <iframe
      src="https://maps.google.com/maps?q=astana&t=&z=13&ie=UTF8&iwloc=&output=embed"
      allowfullscreen=""
    ></iframe>
  </div>

  <div class="section">
    <h2>Подтвердите своё присутствие</h2>
    <a class="rsvp" href="https://forms.gle/YOUR_GOOGLE_FORM" target="_blank">Я приду!</a>
  </div>

  <script>
    // Таймер до даты
    const eventDate = new Date("2025-08-25T17:00:00");
    const timerEl = document.getElementById("timer");

    function updateTimer() {
      const now = new Date();
      const diff = eventDate - now;

      if (diff <= 0) {
        timerEl.innerText = "До встречи на свадьбе!";
        return;
      }

      const days = Math.floor(diff / (1000 * 60 * 60 * 24));
      const hours = Math.floor((diff / (1000 * 60 * 60)) % 24);
      const minutes = Math.floor((diff / (1000 * 60)) % 60);
      const seconds = Math.floor((diff / 1000) % 60);

      timerEl.innerText = `${days}д ${hours}ч ${minutes}м ${seconds}с`;
    }

    setInterval(updateTimer, 1000);
    updateTimer();
  </script>
</body>
</html>
